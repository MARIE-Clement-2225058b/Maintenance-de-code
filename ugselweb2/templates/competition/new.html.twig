{# templates/competition/new.html.twig #}
{% extends 'base.html.twig' %}

{% block body %}
    <h1>Add New Competition</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">Save</button>
    {{ form_end(form) }}
{% endblock %}